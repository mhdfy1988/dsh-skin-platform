# dsh-skin-void-whisper

“虚空低语”是 `dsh-skin-runtime` 的固定深色虚空秘仪风格皮肤。它通过官方主题令牌形成蓝黑表面、冷紫细边线、分层灰白文字和低亮度选中状态，并附带原创抽象 SVG 背景，不直接操作 Harness DOM。

用户选择的本机背景仍可覆盖包内背景，同时保留这套控件配色。

工作区基础表面保持半透明；侧边栏、输入框和菜单仍使用较深的独立表面，避免人物背景影响操作可读性。
